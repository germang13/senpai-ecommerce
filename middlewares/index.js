// ESTO ES SOLO UN EJEMPLO. NO SE VA A USAR
const {
  initalLogging,
  finalLogging,
} = require("./request-tracking.middleware");

const requestTrackingMiddleware = require("./request-tracking.middleware");

module.exports = { initalLogging, finalLogging, requestTrackingMiddleware };
